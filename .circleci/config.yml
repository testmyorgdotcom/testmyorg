version: 2.1

orbs:
  circleci-maven-release-orb: sonatype-nexus-community/circleci-maven-release-orb@0.0.16  

jobs:
  build:
    docker:
      - image: cimg/openjdk:8.0
    steps:
      - run: 
          name: import GPG key
          command: echo -e "$GPG_KEY" | gpg2 --import        
      # - checkout
      # - run: mvn clean deploy -T 1C -P sonatype
